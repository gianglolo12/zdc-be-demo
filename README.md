# zdc-be-demo
Demo backend service for the ZDC harness live workflow demo.
Push a commit to a feature branch with a `[zdc:update-be PRD-xxx]` tag touching `po/**`
to trigger the harness (impact analysis → draft PR).
